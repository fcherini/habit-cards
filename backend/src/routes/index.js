const express = require("express");
const router = express.Router();

// Import controllers
// const myController = require('../controllers/myController');

// Define routes
// router.get('/example', myController.exampleFunction);

module.exports = router;
